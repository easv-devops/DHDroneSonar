kind: pipeline
type: docker
name: integration

steps:
  - name: build and test
    image: mcr.microsoft.com/dotnet/sdk:latest
    commands:
      - cd api && dotnet build
      - cd test && dotnet test
